import {scheduleText} from './morse-engine.js';
function wavBlob(buffer){const ch=buffer.numberOfChannels,sr=buffer.sampleRate,len=buffer.length*ch*2,ab=new ArrayBuffer(44+len),v=new DataView(ab);const ws=(o,s)=>[...s].forEach((c,i)=>v.setUint8(o+i,c.charCodeAt(0)));ws(0,'RIFF');v.setUint32(4,36+len,true);ws(8,'WAVEfmt ');v.setUint32(16,16,true);v.setUint16(20,1,true);v.setUint16(22,ch,true);v.setUint32(24,sr,true);v.setUint32(28,sr*ch*2,true);v.setUint16(32,ch*2,true);v.setUint16(34,16,true);ws(36,'data');v.setUint32(40,len,true);let o=44;for(let i=0;i<buffer.length;i++)for(let c=0;c<ch;c++){const s=Math.max(-1,Math.min(1,buffer.getChannelData(c)[i]));v.setInt16(o,s<0?s*32768:s*32767,true);o+=2}return new Blob([ab],{type:'audio/wav'})}
export async function renderOffline(tl,voice,{lang='es',gender='female',tone=700}={}){const sr=44100,ctx=new OfflineAudioContext(1,Math.ceil((tl.duration+.5)*sr),sr),master=ctx.createGain();master.gain.value=.82;const comp=ctx.createDynamicsCompressor();comp.threshold.value=-3;comp.ratio.value=8;master.connect(comp).connect(ctx.destination);for(const e of tl.events){if(e.type==='cw')scheduleText(ctx,master,e.data.text,e.start,{wpm:e.data.wpm||18,effectiveWpm:e.data.eff||18,tone:e.data.tone||tone,amp:.32});else if(e.type==='voice'||e.type==='charVoice'){const id=e.type==='voice'?e.data.id:`char_${String(e.data.char).toLowerCase()}`;const b=await voice.buffer(ctx,id,lang,gender);if(b){const s=ctx.createBufferSource();s.buffer=b;s.connect(master);s.start(e.start)}}}return ctx.startRendering()}
export async function exportWav(tl,voice,o){return wavBlob(await renderOffline(tl,voice,o))}
export async function exportMp3(tl,voice,o){if(!window.lamejs)throw new Error('MP3 encoder unavailable');const b=await renderOffline(tl,voice,o),pcm=b.getChannelData(0),enc=new lamejs.Mp3Encoder(1,b.sampleRate,128),chunks=[];const block=1152;for(let i=0;i<pcm.length;i+=block){const n=Math.min(block,pcm.length-i),arr=new Int16Array(n);for(let j=0;j<n;j++)arr[j]=Math.max(-32768,Math.min(32767,pcm[i+j]*32767));const mp3=enc.encodeBuffer(arr);if(mp3.length)chunks.push(mp3)}const end=enc.flush();if(end.length)chunks.push(end);return new Blob(chunks,{type:'audio/mpeg'})}
export function download(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),4000)}
